# task.md — active execution only

## Status

`IDLE` — no shared work item or writer lease is active.

## Current work

None. Do not create a competing plan or task file. When new work is authorized, record its scope
proof, deliverables, non-goals, next safe action, verification contract, blockers, and lease here.

## 🔒 Lease — Claude Code ↔ Codex CLI

> Add one scoped row before a shared write. Read-only work may run in parallel. A handoff requires
> stop writing → checkpoint → release old lease → acquire new lease.

| Agent/runtime | File set | Started (ICT) | Purpose |
|---|---|---|---|

## Next safe action

Open only `TL-M0` readiness work allowed by
`docs Toplink/TOPLINK_PAGE_MILESTONES.md`. Preserve all external targets, legal/product evidence,
and professional-review dependencies as blocked until their canonical gates are met.

## Carry-forward blockers
